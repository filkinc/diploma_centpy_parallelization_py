centpy_on_jax (черновой вариант)
 - __init__.py
 - core.py # Определение типов данных (Pars1d, Equation1d, ...) - "Контейнеры"
 - equations.py # "Библиотека" готовых уравнений (Бюргерс, Эйлер ...)
 - boundaries.py # Граничные условия
 - geometry.py # Функции создания сетки и обработки границ (+ добавление сетки)
 - schemes.py # Численные методы (SD2, SD3). Принимают (u, pars), возвращают du/dt
 - time_integration.py # Интеграторы (Runge-Kutta). Принимают функцию правой части.
 - solver.py # "Оркестратор", который собирает всё воедино и запускает цикл

В centpy (ООП): метод меняет внутреннее состояние объекта.
В jax_centpy (функциональное программирование): функция принимает старые данные и возвращает новые. Она чистая, не меняет ничего снаружи.


Дополнительные комментарии к файлам:

limiters.py
На выбор дается несколько лимитеров, чуть подробнее о них
"""
    Классический MinMod лимитер. Самый 'диффузный' (размывает решение), но самый надежный.
    Выбирает наименьший наклон по модулю, если знаки совпадают. Иначе 0.
"""
"""
    Superbee лимитер. Хорошо держит крутые фронты (меньше размывает),
    но может превращать гладкие волны в 'ступеньки' (squaring effect).
    Формула: maxmod(minmod(a, 2b), minmod(2a, b))
    # Возвращаем тот, что больше по модулю, сохраняя знак (знаки у sb1 и sb2 всегда совпадают с a и b)
"""
"""
    MC (Monotonized Central) лимитер. Часто считается 'золотой серединой'.
    lim = minmod(2a, 2b, 0.5*(a+b))
    # Применяем ограничения: не больше 2*left и не больше 2*right
    # Используем логику minmod для трех аргументов:
    # 1. Если знаки a и b разные -> 0
    # 2. Иначе берем min(|2a|, |2b|, |central|) * sign(a)
"""
"""
    Van Leer лимитер. Гладкий лимитер (в отличие от minmod/superbee, у которых есть изломы).
    Формула: (a*|b| + |a|*b) / (|a| + |b|)
    # Добавляем epsilon, чтобы не делить на ноль, если градиенты нулевые
"""

time_integration.py
На выбор дается несколько методов Рунге-Кутты (метод Эйлера лучше не использовать)

"""
    Классический метод Эйлера (1-й порядок).
    u_{n+1} = u_n + dt * L(u_n)
"""
"""
    SSP-RK2 (Метод Хойна / Heun's method). 2-й порядок точности.
    Выпуклая комбинация двух шагов Эйлера.
    # Формула: 0.5*u_n + 0.5*u_1 + 0.5*dt*L(u_1)
    # Или проще: u + 0.5 * dt * (L1 + L2)
"""
"""
    SSP-RK3 (Оптимальный метод 3-го порядка для гиперболических задач).

"""